apiVersion: v1
kind: Service
metadata:
  name: lab-memory-mcp
  namespace: lab-memory
  labels:
    app.kubernetes.io/name: lab-memory-mcp
spec:
  selector:
    app.kubernetes.io/name: lab-memory-mcp
  ports:
    - name: mcp
      port: 8765
      targetPort: mcp
